# Data layout

- `raw/` contains immutable source pulls from nba_api and Basketball-Reference.
- `processed/` contains cleaned tables and model-ready feature sets.

Do not commit large datasets. Add local-only files to `.gitignore` as needed.
